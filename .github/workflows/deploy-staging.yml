name: Deploy Staging
on:
  push:
    branches:
      - staging
jobs:
  deploy:
    runs-on: self-hosted  # 👈 This is the most important change
    steps:
      - name: Run Deployment Script
        run: |
          cd ~/urbanwatch/uat-api
          ./deploy-staging.sh